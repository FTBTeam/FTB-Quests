dependencies {
    minecraft "com.mojang:minecraft:${rootProject.architectury.minecraft}"
    mappings minecraft.officialMojangMappings()
    // We depend on fabric loader here to use the fabric @Environment annotations
    // Do NOT use other classes from fabric loader
    modCompile "net.fabricmc:fabric-loader:${rootProject.fabric_loader_version}"
    modCompile "me.shedaniel:architectury:${rootProject.architectury_version}"
    modCompile "com.feed_the_beast.mods:FTB-Teams:${rootProject.ftb_teams_version}"
    modCompile "com.feed_the_beast.mods:FTB-GUI-Library:${rootProject.ftb_gui_library_version}"
    modCompile "curse.maven:kubejs-fabric-395864:3174959"
    compileOnly "com.google.code.findbugs:jsr305:3.+"
}

architectury {
    common()
}
